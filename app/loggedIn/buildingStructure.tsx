import React from 'react';
import { StyleSheet, View } from 'react-native';
import Building from '../../components/screens/Building';

const BuildingStructure = () => {
    return (
        <Building/>
    );
}

const styles = StyleSheet.create({})

export default BuildingStructure;


