import React from 'react';
import { StyleSheet, View } from 'react-native';
import ProtocolsView from '../../components/screens/ProtocolsView';

const Protocols = () => {
    return (
        <ProtocolsView/>
    );
}

const styles = StyleSheet.create({})

export default Protocols;

