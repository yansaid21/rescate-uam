import React from 'react';
import { StyleSheet, View } from 'react-native';
import MeetPointsList from '../../components/screens/MeetPointsList';

const MeetPoints = () => {
    return (
        <MeetPointsList/>
    );
}

const styles = StyleSheet.create({})

export default MeetPoints;
