import React from "react";
import { StyleSheet } from "react-native";
import ProtocolsMenu from "../../components/screens/ProtocolsMenu";

const protocolsMenu = () => {
    return(
        <ProtocolsMenu/>
    );
}

const styles = StyleSheet.create({});

export default ProtocolsMenu;