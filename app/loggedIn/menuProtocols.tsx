import React from "react";
import { StyleSheet, View} from "react-native";
import MenuProtocols from "../../components/screens/MenuProtocols";

const menuProtocols = () => {
    return(
        <MenuProtocols/>
    )
}

export default MenuProtocols;